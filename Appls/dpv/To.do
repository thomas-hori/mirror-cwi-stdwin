Incorporate Robbert's changes to arcs (see ../ndpv)

Re-read the input (so user can rerun ditroff w/o need to restart dpv)

Read the print menu from a file

Add ditroff new names to funny char table

Show external page numbers; use external page numbers in GOTO command
